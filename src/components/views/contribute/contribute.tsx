import styles from './contribute.module.css';

interface ContributeViewProps {}

const ContributeView = ({}: ContributeViewProps) => {
	return (
		<>
			<div className={styles.contribute}>Contribution View</div>
		</>
	);
};

export default ContributeView;
