import { IMergedStack } from '@/types/stack';

interface ProfileViewViewProps {
	mergedStacks: IMergedStack[];
}

const ProfileViewView = ({}: ProfileViewViewProps) => {
	return (
		<>
			<div>ProfileViewView</div>
		</>
	);
};

export default ProfileViewView;
